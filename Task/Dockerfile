# שלב ראשון: בחירת תמונת בסיס
FROM node:14

# שלב שני: קביעת ספריית העבודה
WORKDIR /app

# שלב שלישי: העתקת קובצי הפרויקט
COPY package*.json ./
COPY . .

# שלב רביעי: התקנת תלותים
RUN npm install

# שלב חמישי: הגדרת פקודת ברירת המחדל להפעלת הסקריפט
CMD ["node", "app.js"]

